import { Download, Plus, Search } from "lucide-react";
import { PageHeader } from "./page-header";
type Row = Record<string, string | number>;
export function DataTablePage({ title, description, button, columns, rows }: { title:string; description:string; button:string; columns:string[]; rows:Row[] }) { return <><PageHeader title={title} description={description} action={<button className="btn-primary"><Plus size={17}/>{button}</button>}/><div className="panel overflow-hidden"><div className="flex flex-wrap items-center justify-between gap-3 p-5"><div className="flex w-full max-w-sm items-center gap-2 rounded-xl border px-3 py-2.5"><Search size={17} className="text-slate-400"/><input className="w-full bg-transparent text-sm outline-none placeholder:text-slate-400" placeholder={`Search ${title.toLowerCase()}...`}/></div><button className="inline-flex items-center gap-2 rounded-xl border px-3 py-2 text-sm font-semibold text-slate-600 dark:text-slate-300"><Download size={16}/>Export</button></div><div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="border-y bg-slate-50 text-xs uppercase tracking-wide text-slate-500 dark:bg-slate-800/40"><tr>{columns.map(c=><th key={c} className="whitespace-nowrap px-6 py-4 font-semibold">{c}</th>)}</tr></thead><tbody>{rows.map((row,i)=><tr key={i} className="border-b last:border-0 hover:bg-slate-50/80 dark:hover:bg-slate-800/30">{columns.map(c=><td key={c} className="whitespace-nowrap px-6 py-4 text-slate-600 dark:text-slate-300">{String(row[c] ?? "—")}</td>)}</tr>)}</tbody></table></div></div></>; }
