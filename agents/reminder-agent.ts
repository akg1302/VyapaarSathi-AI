import type { BusinessAgent, AgentContext } from "@/types/agent";
import { result } from "./agent-utils";
export class ReminderAgent implements BusinessAgent { readonly agentName="reminder" as const; async execute(context: AgentContext) { const start=Date.now(); const pending=context.reminders.filter(r=>r.status==="pending"); const overdue=pending.filter(r=>new Date(r.due_at)<context.now); const recommendations=overdue.map(r=>({priority:"high" as const,title:`Overdue reminder: ${r.title}`,detail:`Due ${new Date(r.due_at).toLocaleDateString("en-IN")}.`,action:"Complete reminder"})); return result("reminder",start,{pendingCount:pending.length,overdueCount:overdue.length,upcoming:pending.slice(0,5).map(r=>({id:r.id,title:r.title,dueAt:r.due_at,priority:r.priority}))},[`Found ${pending.length} pending reminders.`,`${overdue.length} reminder(s) are past their due time.`],recommendations,pending.length ? .98 : .8); } }
